#include <stdio.h>
#include <stdlib.h>


char ** buscar_str(char ** S, int n, char * P){
	
    //for palabra(linea) in archivo
    for(int i = 0;i < n; i++){

   	}
    return lmao
}

int main(){
	//Abrir archivo fp (P)
    //Abrir archivo fs (S)

    //for palabra_P in fp
	for(int i = 0;i < k;i++){
        los_que_estan = buscar_str(fs, len(palabra_S), palabra_P)

        //Crear archivo
        for palabra in los_que_estan:
            archivo.write(palabra\n)
	}		
	buscar_str();	
}
